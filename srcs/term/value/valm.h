#ifndef __VALM__
#define __VALM__

#define NUM(v) ((v)->num)
#define ID(v) ((v)->id)
#define STR(v) ((v)->str)
#define ELM(v) ((v)->elems)
#define KVAL(v) ((v)->kvals)

#endif /* __VALM__ */
